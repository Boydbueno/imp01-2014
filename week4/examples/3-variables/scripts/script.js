// Declaration (creating the variable carFuel)
var carFuel;
console.log(carFuel); // undefined (because it doesn't have a value yet)

// Initialisation (filling the variable carFuel with a value)
carFuel = 10;
console.log(carFuel); // 10;

// Combined
var maxSpeed = 160;
console.log(maxSpeed); // 160